void main() {
  /// Cuando se esta desarrollando es dificil encontrar los errores por lo cual
  /// se utilizan los assert para encontrarlos de una manera mas facil
  int number = 14;
  assert(number != 14);

  /// Para ejecutar este código en la terminal se necesita agregar
  /// el flag --enable-asserts
}
