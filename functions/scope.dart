String variableGlobal = "Hola, soy una variable global";

void main() {
  String variableLocal =
      "Hola, soy una variable que solo funciona dentro de la función que fui declarada";
  print(variableLocal);
  print(variableGlobal);
}
